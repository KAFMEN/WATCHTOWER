%
{% extends "admin/base_admin.html.twig" %}

{% block body %}

	<div class="container py-5">
		<div class="row">
        <div class="col-12 mb-3">
        <a class="btn btn-dark" href="{{ path('admin_category_index') }}">back to list</a>
        </div>
			<div class="col-12">
				<div class="card">
					<div class="card-header">
						<h5>ajouter une category</h5>
					</div>

					<div class="card-body">
						{{ include('admin/category/_form.html.twig') }}
					</div>
				</div>
			</div>
		</div>
	</div>


	
{% endblock %}
